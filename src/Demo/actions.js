import * as types from './constants';

// const succeeded = () => ({
//   type: types.LOAD_OK,
// });
//
// const failed = () => ({
//   type: types.LOAD_FAIL,
// });

export default () => ({
  type: types.LOAD,
});
